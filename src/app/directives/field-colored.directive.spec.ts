import { FieldColoredDirective } from './field-colored.directive';

describe('FieldColoredDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldColoredDirective();
    expect(directive).toBeTruthy();
  });
});
